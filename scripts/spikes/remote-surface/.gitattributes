# Preserve captured terminal control bytes without text conversion or line diffs.
evidence/*.vt binary
evidence/*.bin binary
# Unified patches use a single space for blank context lines.
ghostty-snapshot.patch whitespace=-blank-at-eol
